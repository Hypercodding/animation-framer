export type Step1Inputs = {
    firstName: string;
    lastName: string;
  };
  
export type Step2Inputs = {
    email: string;
    password: string;
  };
  
export type Step3Inputs = {
    address: string;
    phone: string;
  };